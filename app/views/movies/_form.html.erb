<div class="container">
	<%= simple_form_for @movie do |f| %>
	  <%= f.input :rating, label: "Movie Rating", input_html: {value: f.object.rating || '10'}%>
	  <%= f.button :submit, label: "ADD" %>
	<% end %>
</div>
